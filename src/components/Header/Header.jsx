import React, { useEffect, useRef, useState } from 'react';
import Nav from './Nav'

const Header = () => {
  return (
    <header>
      <h1>PokeApp</h1>
      <Nav />
    </header>)
}

export default Header;
